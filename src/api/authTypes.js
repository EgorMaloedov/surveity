/**
 * Type for representing the registration response.
 * @typedef {Object} RegisterResponse
 * @property {string} accessToken - Access token.
 */

/**
 * Type for representing the login response.
 * @typedef {Object} LoginResponse
 * @property {string} accessToken - Access token.
 */

/**
 * Type for representing the authentication response.
 * @typedef {Object} AuthenticateResponse
 * @property {string} message - Successful authentication message.
 */
